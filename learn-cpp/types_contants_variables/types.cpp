#include <iostream>

// Cpp has various types such as bool, char, wchar_t, int, float, double, short,
// long, long double
//

using namespace std;

int main() {
  bool a = true;  // one bit
  bool b = false; // one bit

  char x;    // for ANSI character set 8 bit
  wchar_t y; // for Unicode set 16 bit
             // You can study other types from google
    int m; // integral data types are tailor made i.e. they adapts to the length of computer register
    // for a 16 bit computer the size of int is 16. For a 32 bit computer the size of int is 32, etc.

  return 0;
}
