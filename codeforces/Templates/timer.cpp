#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    auto start = chrono::high_resolution_clock::now();

    // ... solution ...

    auto end = chrono::high_resolution_clock::now();
    cerr << "Time: " << chrono::duration<double, milli>(end - start).count() << " ms\n";

    return 0;
}
