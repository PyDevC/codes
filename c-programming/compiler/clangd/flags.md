# flags

## flags to control warnings and error messages
`-Werror` turns a warning into error
`-Werror=warn` let warn be a warning name then -Werror will convert it into an error 
`-Wno-error=warn` even if `-Werror` is specified, it will not be converted into error
`-pedantic` warn on language extension
